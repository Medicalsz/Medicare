{% extends 'admin/base.html.twig' %}

{% block title %}Modifier Collaboration{% endblock %}

{% block content %}
<div class="container py-4">
    <div class="row justify-content-center">
        <div class="col-lg-8">
            <div class="d-flex align-items-center justify-content-between mb-4">
                <div class="d-flex align-items-center">
                    <a href="{{ path('app_collaboration_index') }}" class="btn btn-outline-secondary btn-sm rounded-circle me-3">
                        <i class="bi bi-arrow-left"></i>
                    </a>
                    <h1 class="h3 mb-0 text-gray-800">Modifier la Collaboration</h1>
                </div>
                {{ include('collaboration/_delete_form.html.twig', {'button_class': 'btn btn-outline-danger btn-sm'}) }}
            </div>

            <div class="card shadow-sm border-0 rounded-4">
        <div class="card-body p-4">
            {{ form_errors(form) }}
            {{ include('collaboration/_form.html.twig', {'button_label': 'Mettre à jour'}) }}
        </div>
    </div>
        </div>
    </div>
</div>
{% endblock %}