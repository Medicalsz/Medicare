{% extends 'admin_base.html.twig' %}

{% block title %}Modifier Partenaire{% endblock %}

{% block body %}
<div class="container py-4">
    <div class="row justify-content-center">
        <div class="col-lg-8">
            <div class="d-flex align-items-center justify-content-between mb-4">
                <div class="d-flex align-items-center">
                    <a href="{{ path('app_partner_index') }}" class="btn btn-outline-secondary btn-sm rounded-circle me-3">
                        <i class="bi bi-arrow-left"></i>
                    </a>
                    <h1 class="h3 mb-0 text-gray-800">Modifier {{ partner.name }}</h1>
                </div>
                {{ include('partner/_delete_form.html.twig', {'button_class': 'btn btn-outline-danger btn-sm'}) }}
            </div>

            <div class="card shadow-sm border-0 rounded-4">
        <div class="card-body p-4">
            {{ form_errors(form) }}
            {{ include('partner/_form.html.twig', {'button_label': 'Mettre à jour'}) }}
        </div>
    </div>
        </div>
    </div>
</div>
{% endblock %}