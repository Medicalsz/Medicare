{% extends 'dashboard_base.html.twig' %}

{% block title %}Modifier le sujet - Medicare{% endblock %}

{% block body %}
<div class="dashboard-content">
    <div class="page-header">
        <div>
            <h1 class="page-title">Modifier le sujet</h1>
            <p class="page-subtitle">Mettez à jour votre publication.</p>
        </div>
        <a href="{{ path('dashboard_forum') }}" class="btn btn-secondary">Retour au forum</a>
    </div>

    <div class="card">
        {{ form_start(form) }}
            {{ form_row(form.title) }}
            {{ form_row(form.content) }}

            <div style="margin-top: 15px;">
                <button class="btn btn-primary" type="submit">Mettre à jour</button>
                <a href="{{ path('forum_show', {id: topic.id}) }}" class="btn btn-secondary" style="margin-left: 10px;">Annuler</a>
            </div>
        {{ form_end(form) }}
    </div>
</div>
{% endblock %}
